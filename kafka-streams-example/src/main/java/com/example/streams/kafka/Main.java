package com.example.streams.kafka;

import com.example.streams.kafka.stream.BasicStream;
import com.example.streams.kafka.stream.WordCount;
import com.example.streams.kafka.stream.WordInTextCount;

public class Main {
    public static void main(String[] args) throws InterruptedException {
        // basic
//        BasicStream basicStream = new BasicStream();
//        basicStream.start();

        // word count
//        WordCount wordCount = new WordCount();
//        wordCount.start();

        // word in text count
//        WordInTextCount wordInTextCount = new WordInTextCount();
//        wordInTextCount.start();
    }
}
